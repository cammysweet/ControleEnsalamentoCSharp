@model IEnumerable<ControlesEnsalamento.Models.TipoSalas>

@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idTipoSala)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricaoSala)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idTipoSala)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricaoSala)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTipoSala }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTipoSala }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTipoSala })
        </td>
    </tr>
}

</table>
